import { Button, Container } from '../styles/EmoEx/EmoEx';

const EmoEx = () => {
  return (
    <Container column>
      <Button>This is a regular button.</Button>
      <Button primary>This is a primary button.</Button>
    </Container>
  );
};

export default EmoEx;
